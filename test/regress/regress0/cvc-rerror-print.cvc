% EXPECT: entailed
% EXPECT: Cannot get model, since an exception was raised: Cannot check if model core symbol unless after a SAT or unknown response.
OPTION "logic" "ALL";
OPTION "produce-models" true;
x : INT;
QUERY x = x;
COUNTEREXAMPLE;
