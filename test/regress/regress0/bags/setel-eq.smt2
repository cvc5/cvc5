(set-logic ALL)
(set-info :status unsat)
(declare-fun S () (Bag Int))
(declare-fun T () (Bag Int))
(declare-fun x () Int)
(declare-fun y () Int)
(assert (>= (bag.count y S) 1))
(assert (not (>= (bag.count x (bag.union_disjoint S T)) 1)))
(assert (= x y))
(check-sat)
