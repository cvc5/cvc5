(set-logic ALL)
(set-info :status unsat)
(define-sort SetInt () (Bag Int))
(declare-fun a () (Bag Int))
(declare-fun b () (Bag Int))
(declare-fun x () Int)
;(assert (not (>= (bag.count x a)))
(assert (>= (bag.count x (bag.inter_min a b)) 1))
(assert (not (>= (bag.count x b) 1)))
(check-sat)
(exit)
