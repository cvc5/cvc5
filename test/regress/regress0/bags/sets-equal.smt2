(set-logic ALL)
(set-info :status unsat)
(declare-fun x () Int)
(declare-fun y () Int)
(declare-fun z () Int)
(assert (= x y))
(declare-fun a () (Bag Int))
(declare-fun b () (Bag Int))
(assert (not (>= (bag.count x a) 1)))
(assert (>= (bag.count y (bag.union_disjoint a b)) 1))
(assert (= x z))
(assert (not (>= (bag.count z a) 1)))
(assert (= a b))
(check-sat)
