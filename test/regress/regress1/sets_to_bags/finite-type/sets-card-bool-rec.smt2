(set-logic QF_ALL)
(set-info :status sat)
(set-option :sets-ext true)
(declare-fun A () (Bag Bool))
(declare-fun x () Bool)
(assert (>= (bag.count (>= (bag.count x A) 1) A) 1))
(assert (> (bag.card A) 1))
(declare-fun U () (Bag Bool))
(assert (= U (as set.universe (Bag Bool))))
(check-sat)
