(set-logic QF_ALL)
(set-info :status sat)
(set-option :produce-models true)
(set-option :sets-ext true)
(declare-datatype Color ((Red) (Green) (Blue)))
(declare-fun A () (Bag (Array Color Color)))
(declare-fun B () (Bag (Array Color Color)))
(assert (> (bag.card A) 25))
(assert (> (bag.card B) 25))
(assert (distinct A B))
(check-sat)
