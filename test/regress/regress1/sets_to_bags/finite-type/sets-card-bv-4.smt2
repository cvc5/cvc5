(set-logic ALL)
(set-info :status unsat)
(set-option :produce-models true)
(set-option :sets-ext true)
(declare-fun A () (Bag (_ BitVec 3)))
(declare-fun B () (Bag (_ BitVec 3)))
(declare-fun universe () (Bag (_ BitVec 3)))
(declare-fun x () (_ BitVec 3))
(assert (= (bag.card A) 5))
(assert (= (bag.card B) 5))
(assert (not (= A B)))
(assert (= (bag.card (bag.inter_min A B)) 2))
(assert (= (bag.card (bag.difference_remove A B)) 3))
(assert (= (bag.card (bag.difference_remove B A)) 3))
(assert (= universe (as set.universe (Bag (_ BitVec 3)))))
(assert (not (bag.count x A)))
(assert (not (bag.count x B)))
(check-sat)
