(set-logic QF_ALL)
(set-info :status sat)
(set-option :sets-ext true)
(declare-const universe (Bag (Array Int Int)))
(declare-const A (Bag (Array Int Int)))
(declare-const B (Bag (Array Int Int)))
(assert (= (bag.card universe) 3))
(assert (= (bag.card A) 1))
(assert (= (bag.card B) 2))
(assert (= (bag.inter_min A B) (as bag.empty (Bag (Array Int Int)))))
(assert (= universe (as set.universe (Bag (Array Int Int)))))
(check-sat)
