(set-logic QF_ALL)
(set-option :sets-ext true)
(set-info :status sat)
(assert (= (bag.card (as set.universe (Bag Int))) 10))
(declare-const universe (Bag Int))
(declare-const A (Bag Int))
(declare-const B (Bag Int))
(assert (= (bag.card A) 5))
(assert (= (bag.card B) 5))
(assert (= (bag.inter_min A B) (as bag.empty (Bag Int))))
(assert (= universe (as set.universe (Bag Int))))
(check-sat)
