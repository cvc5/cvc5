(set-logic ALL)
(set-info :status sat)
;(set-option :sygus-inference true)
;(set-option :sets-infer-as-lemmas false)
(declare-fun a () (Bag Int))
(declare-fun b () (Bag Int))
(declare-fun c () (Bag Int))
(declare-fun d () Int)
(assert (> (bag.card (bag.difference_remove a (bag.inter_min (bag.difference_remove a b) (bag.difference_remove a c)))) d))
(check-sat)
