(set-logic ALL)
(set-info :status sat)
(declare-fun s () (Bag Real))
(declare-fun t3 () (Bag Real))
(assert (or (>= (bag.count 1.0 t3) 1) (>= (bag.count 0.0 s) 1)))
(assert (not (= t3 (bag.difference_remove s (bag 1.0 1)))))
(check-sat)
