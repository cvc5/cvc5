(set-logic HO_ALL)
(set-option :produce-models true)
(set-option :dag-thresh 0)
(set-info :status sat)
(declare-const A (Relation String Int))
(declare-const B (Relation String Int))
(declare-const x Int)
(declare-const z Int)
(define-fun min ((x Int) (y Int)) Bool (<= x y))
(assert (= x ((_ rel.min 1) min (set.inter A B) -1000)))
(assert (= z ((_ rel.max 1) min (set.inter A B) 1000)))
(assert (not (set.is_empty (set.inter A B))))
(assert (distinct x z))
(check-sat)
