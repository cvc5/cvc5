(set-logic HO_ALL)
(set-option :produce-models true)
(set-option :dag-thresh 0)
(set-info :status sat)
(declare-const A (Set Int))
(declare-const B (Set Int))
(declare-const x Int)
(declare-const y Int)
(declare-const z Int)
(define-fun min ((x Int) (y Int)) Bool (<= x y))
(assert (= x (set.min min (set.inter A B) -1000)))
(assert (= z (set.max min (set.inter A B) 1000)))
(assert (not (set.is_empty (set.inter A B))))
(assert (set.member y (set.inter A B)))
(assert (set.member z (set.inter A B)))
(assert (distinct x y z))
(check-sat)

