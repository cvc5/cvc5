
; temporary
(define-rule bv-eq-symm ((t ?BitVec) (s ?BitVec)) (= t s) (= s t))

;(define-cond-rule ((x ?BitVec) (amount ?BitVec))
;    (< (bv2nat amount) (bvsize x))
;    (bvshl x amount)
;    (concat (extract (bvsize x) (- (bvsize x) 1 (bv2nat amount)) x) (bv 0 amount)))

(define-rule bv-extract-combine ((x ?BitVec) (i Int) (j Int) (k Int) (l Int))
  (extract i j (extract k l x))
  (extract (+ i l) (+ j l) x))
  
