export enum drawerHelpersKind {
    RULE,
    ARGS,
    CONC,
    ALL,
}

export enum renderLetKind {
    EXPAND,
    REVERT,
}

export enum ClusterKind {
    NONE,
    SAT,
    CNF,
    TL,
    PP,
    IN,
}
